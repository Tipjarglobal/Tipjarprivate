import React from "react";
import SponsorFeeder from "./SponsorFeeder";

// RASTER 1 — Rent Pills oben (Top Partner / Sponsoren). Kapselt den bestehenden SponsorFeeder.
export default function Raster1_RentPills(props) {
  return <SponsorFeeder {...props} />;
}
